/**
 * "Beta": a surface that ships but has not yet met every setup it claims —
 * Remote (#242), whose Linux and macOS hosts the suite covers and no real
 * machine has yet. A label, not a control: a hairline in the accent's ink,
 * no fill, so it never reads as something to click.
 */
export function BetaTag() {
  return <span className="cl-beta">Beta</span>;
}
